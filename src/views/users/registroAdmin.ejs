<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head', {title: "- Añadir Administrador", cssFile: "registro", cssHome: ""}) %>
<body>
    <main class="main-registro">
        <div class="contenedor-form">
            <img src="/images/Logo.png" alt="Logo" class="logo">

            <div class="form">
                <div>
                    <h1 class="title"> Agregar nuevo administrador</h1>
                    <p class="subtitle">Completá el formulario para dar de alta</p>
                </div>
                
                <form action="/users/registro-admin" method='POST' class="contenedor-registro">
                    <div class="datos">      
                        <div class="contenedor-datos1">
                            <label for="nombre" class="label">Nombre</label>
                            <input type="text" name="nombre" class="input" placeholder="Nombre" value="<%= locals.oldData && oldData.nombre %>">
                            <% if(locals.errors && errors.nombre) { %>
                                <span class="feedback-negativo"><%= errors.nombre.msg %></span>
                            <% } %> 

                            <label for="email" class="label">Correo</label>
                            <input type="text" name="email" class="input" placeholder="ejemplo@ejemplo.com" value="<%= locals.oldData && oldData.email %>">
                            <% if(locals.errors && errors.email) { %>
                                <span class="feedback-negativo"><%= errors.email.msg %></span>
                            <% } %> 
                        </div>
                        
                        <div class="contenedor-datos2">
                            <label for="apellido" class="label">Apellido</label>
                            <input type="text" name="apellido" class="input" placeholder="Apellido" value="<%= locals.oldData && oldData.apellido %>">
                            <% if(locals.errors && errors.apellido) { %>
                                <span class="feedback-negativo"><%= errors.apellido.msg %></span>
                            <% } %> 

                            <label for="contrasenia">Contraseña</label>
                            <input type="password" name="contrasenia" placeholder="Admin123">
                            <% if(locals.errors && errors.contrasenia) { %>
                                <span class="feedback-negativo"><%= errors.contrasenia.msg %></span>
                            <% } %> 
                        </div>
                    </div>
                        
                    <div>
                        <button type="submit" class="boton-registro">Dar de alta</button>
                    </div>
                    <p class="mensaje-alta-admin"><%= locals.mensaje && locals.mensaje %></p>
                        
                </form>
        
                <hr>
            
                <div class="pie-formulario">
                    <a href="/" class="volver-tienda">Volver a la tienda</i></a>
                    <p class="copyright">Copyright © 2023</p>
                </div>
            </div>             
        </div>
    </main>

</body>
</html>