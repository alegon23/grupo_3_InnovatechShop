<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head', {title: "- Iniciar Sesion", cssFile: "login", cssHome: "styles"}) %>
<body>
    <main id="main-login">
        <div class="container">
            <img src="/images/Logo.png" alt="Logo" class="logo-login">

            <div class="caja-formulario">
                <h2>Iniciar Sesión</h2>

                <form action="/users/login" method="POST">
                    <label for="email">Correo Electrónico</label>
                    <input type="text" name="email" placeholder="ejemplo@ejemplo.com" value="<%= locals.oldData && oldData.email %>">
                    <% if(locals.errors && errors.email) { %>
                        <span class="feedback-negativo"><%= errors.email.msg %></span>
                    <% } %>

                    <label for="contrasenia" class="label-password">Contraseña</label>
                    <input type="password" name="contrasenia" placeholder="••••••••••">
                    <% if(locals.errors && errors.contrasenia) { %>
                        <span class="feedback-negativo"><%= errors.contrasenia.msg %></span>
                    <% } %>

                    <button type="submit">Ingresar</button>

                    <div class="recordar-usuario">
                        <input type="checkbox" name="recordar-usuario" value="recuerdame">
                        <label for="recuerdame" class="checkbox-text">Recuérdame</label>
                    </div>
                </form>
    
                <a href="#" class="olvido-password">¿Olvidaste tu contraseña?</a>

                <div class="crear-cuenta">
                    <p>¿No tienes una cuenta?</p>
                    <a href="../users/registro" class="link-registro">Registrate aquí</a>
                </div>
                
            </div>

            <hr>

            <div class="footer-form">
                <a href="#"><i class="bi bi-question-circle"></i> ¿Necesitas ayuda?</a>
                <a href="/" class="volver-tienda">Volver a la tienda</i></a>
                <p>Copyright © 2023</p>
            </div>
        </div>
    </main>
</body>
</html>