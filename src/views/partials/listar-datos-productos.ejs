<%# esto es para listar la descripciones y las caracteristicas de los productos. Tiene ese formato para que se vea bien en el text area %>
<% for (dato of datos) {%><%= dato.title %>
<%= dato.text %>

<% } %>